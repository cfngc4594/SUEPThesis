\chapter{在线编程评测平台核心实现}

\section{整体实现结构}

\begin{figure}[htbp]
  \centering
  \resizebox{\linewidth}{!}{\input{figures/system_architecture.tex}}
  \caption{系统总体架构示意}
  \label{fig:ch5-system-arch}
\end{figure}

本系统如上图架构所示采用全栈开发的模式，前端部分使用 React 框架展示题目描述、题解、测试用例、提交记录和 AI 辅助反馈组件，组件库与样式部分采用 Tailwind 和 Shadcn/UI，通过 Zustand 进行状态管理，后端部分使用 Server Action 接收代码提交后，会先创建可追踪的提交记录，然后再通过 Inngest 调度容器完成代码编译与运行，同时 Vercel AI SDK 会对提交的代码进行分析并生成相应分数，这些内容最终都会写入到 Prisma 管理的数据库中，Prisma 还可以根据表结构生成 TypeScript 类型，不但可以提供开发过程中的类型安全，让前后端使用统一的接口类型，还能非常方便地实现在不同关系型数据库中的迁移。

在具体的判题服务上，本系统参考了 Judge0 与 DOMjudge 等开源项目的 API 接口与相关文档中提到的沙箱边界与运维经验\cite{judge0online,domjudgeonline}，将 Web 服务和判题服务单独部署，还支持本地编译代码和远程编译代码。本地编译时会通过宿主机套接字启动容器进行编译，远程编译时会按照配置里的协议、地址、端口、证书等配置与远程服务器中的 Docker 建立连接，同时系统本身也支持 Docker Compose 一键部署，避免出现环境不兼容的情况。

\section{身份认证与会话管理实现}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{images/fig5-1.png}
  \caption{自定义登录页截图}
  \label{fig:ch5-signin}
\end{figure}

本系统中的不同角色的登录入口统一为 \texttt{/sign-in} 页面，该页面提供了邮箱密码登录和第三方 OAuth 登录两种方式，界面如图~\ref{fig:ch5-signin} 所示，默认情况下，首个注册用户会被赋予管理员角色。

邮箱密码登录需要先在系统中注册账号，点击注册会进行表单校验以及显示对应的用户提示，例如该邮箱已注册、前后密码不一致，同时服务端会进行二次校验。

第三方 OAuth 登录目前接入了 GitHub 与 Google 两种方式，以 GitHub 为例，用户点击对应按钮后浏览器会先跳转到 GitHub 授权页，授权完成后会携带相关 token 进行回调，本系统的认证路由会根据 token 自动进行用户信息查询，如果用户不存在则进行注册，如果用户存在则进行登录，过程如图~\ref{fig:ch5-oauth} 所示。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.78\linewidth]{images/fig5-2.png}
  \caption{GitHub OAuth 授权确认页截图}
  \label{fig:ch5-oauth}
\end{figure}

对本系统而言，两种登录方式共用一套用户与账户表，业务侧不需要区分用户最初来自邮箱注册还是 OAuth 授权，登录成功后，系统会在 Cookie 中保存会话令牌，后续访问系统不需要再次进行登录，同时不同用户角色在某些界面上的权限不同，例如管理员可以访问后台管理页面，教师可以管理题目和课程，学生可以提交代码和查看结果，具体的登录流程如图~\ref{fig:ch5-auth-overview} 所示。

\begin{figure}[!htbp]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/auth_credentials_flow.tex}}
    \caption{邮箱凭据登录与数据库会话建立}
    \label{fig:ch5-auth-credentials-flow}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/auth_oauth_github_flow.tex}}
    \caption{GitHub OAuth 登录与 \texttt{/api/auth/*} 回调处理}
    \label{fig:ch5-auth-github-flow}
  \end{subfigure}
  \caption{平台身份认证主要流程示意}
  \label{fig:ch5-auth-overview}
\end{figure}

\FloatBarrier

\section{核心数据模型}

\begin{figure}[htbp]
  \centering
  \resizebox{\linewidth}{!}{\input{figures/core_er_diagram.tex}}
  \caption{核心数据表实体关系示意}
  \label{fig:ch5-er}
\end{figure}

图~\ref{fig:ch5-er} 概括了本系统的核心数据表之间的关系，User 实体的角色字段用于区分管理员、教师与学生身份，用于确定题目管理权限和后台访问范围。学生对同一个 Problem 可以提交多次代码，产生多条 Submission 记录，Problem 本身也与 Testcase 相关联。每条 Submission 会有一条 Judge 记录用于保存最终判题结果，再由多条 JudgeRun 单独记录每条测试用例的执行结果，CodeAnalysis 则是 Submission 的 AI 辅助分析结果。

\subsection{题目内容与模板数据设计}

Problem 表中的 displayId 用于题号展示，timeLimit 和 memoryLimit 用于单次提交的资源限制。

ProblemLocalization 表可以通过 problemId、locale、type 获取不同语言版本的题目标题、题面、题解文本。

学生在编辑器中答题时，编辑器会根据 problemId 和 language 从 Template 表中获取对应语言的模版代码。

\subsection{提交、判题轨迹与分析结果设计}

根据判题流程，本系统需要记录每一次提交的判题状态、测试用例执行结果和 AI 分析结果，本小节会以表的形式汇总各个核心数据表的字段设计，并结合表中字段说明 \texttt{Submission} 状态码的取值含义以及三者之间的关系。

\begingroup
\small
\setlength{\LTleft}{\fill}
\setlength{\LTright}{\fill}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.15\textwidth}P{0.29\textwidth}P{0.44\textwidth}}
\caption{核心数据表字段设计汇总}\\
\toprule
数据表 & 关键字段 & 主要用途 \\
\midrule
\endfirsthead
\multicolumn{3}{c}{续表\thetable}\\
\toprule
数据表 & 关键字段 & 主要用途 \\
\midrule
\endhead
\bottomrule
\endfoot
User & id, email, password, role, image & 这一表主要把账号和角色这些核心信息落下来，后续在认证鉴权、权限判断、提交归属追踪时都会直接用到。 \\
Problem & id, displayId, difficulty, isPublished, timeLimit, memoryLimit, isTrim & 描述题目的基础约束与评测配置，是题库管理和判题流程的核心实体。 \\
Problem\allowbreak Localization & problemId, locale, type, content & 按语言和内容类型存储题目标题、题面描述与题解等文本信息，支持多语言展示。 \\
Template & problemId, language, content & 保存不同编程语言对应的模板代码，供用户进入题目页后直接加载。 \\
Submission & id, language, content, status, message, timeUsage, memoryUsage, userId, problemId & 该表按提交维度保存代码内容、判题状态、错误信息和资源消耗数据，判题核心流程的多数状态流转都围绕它展开。 \\
Judge & id, submissionId, status, startTime, endTime, compileOutput & 这里会把一次提交在判题阶段的关键轨迹记录下来，编译输出也会被完整保留，方便后面按提交粒度去回溯全过程。 \\
JudgeRun & id, judgeId, testcaseId, status, timeUsage, memoryUsage, stdin, stdout, stderr & 保存逐用例执行结果与输入输出信息，支撑失败定位与详情展示。 \\
Testcase & id, expectedOutput, problemId & 保存标准测试用例及其期望输出，为程序运行结果判定提供依据。 \\
Testcase\allowbreak Input & id, testcaseId, index, name, value & 该表用于保存测试用例的有序输入项，运行阶段会按这个顺序去组装标准输入流。 \\
Code\allowbreak Analysis & id, submissionId, status, timeComplexity, spaceComplexity, overallScore, feedback & 用于保存 AI 对提交代码给出的结构化分析结果，同时与客观判题结果分开存储，避免相互干扰。 \\
\bottomrule
\end{longtable}
\endgroup

\begingroup
\small
\setlength{\LTleft}{\fill}
\setlength{\LTright}{\fill}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.10\textwidth}P{0.20\textwidth}P{0.56\textwidth}}
\caption{Submission 状态码及含义说明}\\
\toprule
状态码 & 含义 & 触发阶段/说明 \\
\midrule
\endfirsthead
\multicolumn{3}{c}{续表\thetable}\\
\toprule
状态码 & 含义 & 触发阶段/说明 \\
\midrule
\endhead
\bottomrule
\endfoot
PD & Pending & 提交记录已创建，处于判题流程起点，尚未进入编译阶段。 \\
QD & Queued & 表示这次提交已经被系统接收并完成持久化，后面会由事件函数异步拉取，继续进入编译和运行流程。 \\
CP & Compiling & 代码已进入编译阶段，系统正在调用对应语言编译器进行构建。 \\
CE & Compilation Error & 编译失败，系统会将编译器错误输出写回 Submission.message 供前端展示。 \\
CS & Compilation Success & 编译成功的中间状态，通常会很快继续流转到运行阶段。 \\
RU & Running & 程序正在受控容器环境中执行测试用例。 \\
AC & Accepted & 所有测试用例均通过，提交被判定为正确结果。 \\
WA & Wrong Answer & 程序可正常运行，但输出结果与标准答案不一致。 \\
RE & Runtime Error & 程序在运行阶段异常退出或发生非法操作。 \\
TLE & Time Limit Exceeded & 说明程序在给定时限内没有跑完，常见原因一般是死循环，或者时间复杂度偏高。 \\
MLE & Memory Limit Exceeded & 程序运行期间内存占用超过题目限定阈值。 \\
SE & System Error & 系统环境、配置或流程执行出现异常，例如题目缺失、Docker 配置缺失或容器调用失败。 \\
\bottomrule
\end{longtable}
\endgroup

Submission 表存了语言类型、源代码、最终判题状态、报错信息、用了多少时间、占了多少内存这些内容。Testcase 和 TestcaseInput 表存着一道题所有的测试用例，Judge 跟 JudgeRun 表则分别记录了每次提交的判题过程，还有每个测试用例最后跑出来的结果。

要拆成 Submission、Judge、JudgeRun 这三层，是为了把提交整体的状态，和单个测试用例的细节分开互不影响，这样就能在列表页先展示提交的概况，点进详情页再看具体是哪个测试用例让判题不通过。CodeAnalysis 表是用来存 AI 辅助分析的结果的，它靠 submissionId 和 Submission 表建立一对一的关联，还用单独的状态字段追踪分析任务现在走到哪个阶段了，这样一来，AI 分析就能放在后台异步跑，不会卡住主判题的流程，分析结果也能和判题结果分开存在不同的表里。

\begingroup
\small
\setlength{\LTleft}{\fill}
\setlength{\LTright}{\fill}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.22\textwidth}P{0.24\textwidth}P{0.42\textwidth}}
\caption{核心数据约束与设计意图}\\
\toprule
约束对象 & 约束形式 & 设计意图 \\
\midrule
\endfirsthead
\multicolumn{3}{c}{续表\thetable}\\
\toprule
约束对象 & 约束形式 & 设计意图 \\
\midrule
\endhead
\bottomrule
\endfoot
ProblemLocalization & \texttt{problemId + locale + type} 复合主键 & 能保证同一道题的同一种内容类型，在同一种语言下只会有唯一版本，避免题面和题解的文本出现冲突。 \\
Template & \texttt{problemId + language} 复合主键 & 能保证每道题目在每种语言下都只存一份模板代码，方便前端切换语言的时候，直接加载对应的初始代码。 \\
CodeAnalysis & \texttt{submissionId} 唯一约束 & 保证一次提交只对应一条主分析记录，不会出现多次分析结果互相覆盖、或者重复展示的问题。 \\
外键级联删除 & 多个实体对 Problem、Submission 的级联关系 & 理清多个实体对 Problem、Submission 的级联关系，当题目或者提交被删除之后，关联的本地化文本、模板、判题轨迹还有分析记录都会跟着被清理，能减少没用的孤立数据残留下来。 \\
提交和用例分层记录 & Submission、Judge、JudgeRun 分层设计 & 既保存了提交级的摘要信息，又保留了用例级的细节内容，可以给详情展示、错误复盘还有统计工作提供粒度更细的数据基础。 \\
\bottomrule
\end{longtable}
\endgroup

\section{题库与题目组织}

题库管理模块主要由 Problem、ProblemLocalization、Template 以及 Testcase 这几张表组成，Problem 表用来存题目的基础信息，ProblemLocalization 表存题目对应的多语言文本，Template 表存不同编程语言的模板代码，Testcase 表则存具体的测试用例。这套设计让题目内容维护起来很方便，教师能通过图5-6的管理界面完成题目管理操作。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-3.png}
  \caption{题库管理页面截图}
  \label{fig:ch5-problem-admin}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-4.png}
  \caption{题目详情页面截图}
  \label{fig:ch5-problem-detail}
\end{figure}

\section{在线提交与容器化判题}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-5.png}
  \caption{代码提交与判题结果页面截图}
  \label{fig:ch5-submit}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-6.png}
  \caption{Inngest 异步判题执行轨迹截图}
  \label{fig:ch5-submission-list}
\end{figure}

\subsection{提交受理与状态流转}

用户点击提交按钮之后，系统会先检查用户的登录状态、确认题目和测试用例是否存在，同时还要核对对应语言的 Docker 配置是不是完整，检查都没问题后，\path{src/app/actions/judge.ts} 会生成一条状态为 \texttt{QD} 的 Submission 记录，再把提交 id 通过事件 \texttt{submissions/create} 发送给 Inngest。到这一步 HTTP 请求就可以返回结果了，接下来的编译和运行都交给异步链路处理，另外，提交入口还会把当前界面使用的语言传给 AI 分析环节，这样反馈里的 \texttt{feedback} 字段就能和用户的语言环境保持统一。

先把 Submission 记录建好，再进入编译运行这一步，能保证用户每一次提交都能留下稳定的记录，前端页面也能立刻展示提交内容。之后不管是异步链路里出现编译出错、运行出错，还是容器调用出错，各类报错信息都会统一回写到这同一条记录里，状态会从 \texttt{QD}、\texttt{CP}、\texttt{RU} 一直更新到最终状态，方便后续排查问题和复盘。

系统同时维护两种状态，一种是提交级状态，另一种是判题轨迹状态：\texttt{Submission.status} 主要用来给前端展示和筛选，\texttt{Judge.status} 和 \texttt{JudgeRun.status} 则负责记下编译过程，还有每个测试用例的执行细节，这两部分会通过统一映射保持状态一致。状态发生变化的时候，会通过广播推送到 problem 频道和 submission 频道，前端监听到变化后就会刷新提交列表和详情页，做到异步执行过程里信息可以连续更新。

\subsection{编译流程实现}

编译阶段是由 \path{src/inngest/functions/process-submission.ts} 调用 \path{src/lib/judge-runtime.ts} 里封装好的运行时来完成的。调用的函数会先把 Submission 的状态改成“编译中”，之后再按照提交代码的语言类型生成对应的编译命令，本文里，C语言和C++分别会用 \texttt{gcc -O2} 和 \texttt{g++ -O2} 编译出可执行文件 \texttt{main}。若编译失败，错误信息写入 \texttt{Submission.message} 和 \texttt{Judge.compileOutput}，状态更新为 Compilation Error。编译器标准错误流被保存后，前端详情页将其渲染为 shell 片段，供学生查看语法或链接错误。

\subsection{运行与用例判定实现}

在运行阶段，\path{process-submission.ts} 调用运行逻辑，按测试用例顺序逐个执行，将排序后的输入参数写入标准输入流，根据程序输出与标准输出的对比判断结果。若题目启用了裁剪空白字符比较，则采用 trim 方式对比，否则进行严格匹配。每个用例的输入、输出、错误输出、时间消耗和状态均保存至 JudgeRun。

与同步串行判题不同，本文将容器准备、源码上传、编译、逐用例执行和容器清理组织为 Inngest 函数内的阶段化步骤，图~\ref{fig:ch5-submission-list} 展示了该链路的阶段轨迹。一旦某用例触发 \texttt{WA}、\texttt{RE}、\texttt{TLE} 或 \texttt{MLE}，系统立即将当前 JudgeRun 落库并停止后续用例执行，只有前一个用例通过才继续下一个。这种短路判题方式减少了无效计算，能更快返回失败结果。

若所有用例通过，系统汇总各用例耗时及容器统计信息，更新最终的时间与内存摘要，并将 Submission 状态设为 Accepted。执行过程中每个用例的输入按 \texttt{index} 排序后拼成标准输入送入容器，输出和耗时单独记录，便于定位首个失败用例。全部通过时，系统从各 JudgeRun 中提取非空时间并取最大值作为本次提交的时间摘要，而非仅取最后一个用例的耗时；内存使用量则借助容器统计信息提取峰值，为页面展示和性能分析提供依据。

\subsection{资源约束与安全隔离实现}

容器创建逻辑中，系统为每次判题准备独立运行环境，设置工作目录和内存限制，并默认禁用网络，以降低恶意代码或异常程序对宿主系统的影响。每次提交都在新的容器环境中完成编译和运行，保证了不同提交之间的环境隔离和执行一致性。

判题结束后，无论成功还是失败，系统都会停止并删除对应容器，避免临时执行环境在宿主机上持续堆积。这种单次提交对应单次执行环境、执行后立即回收的策略，虽然会带来一定容器启动开销，但换取了更清晰的故障边界和更低的残留风险。

\subsection{判题异常路径、错误回写与可观测性}

除编译失败、答案错误和运行超时等学生侧常见结果外，系统还需覆盖一类系统侧异常，如 Docker 守护进程不可用、镜像缺失、题目或测试数据配置不完整、远程 Docker 连接失败等。此类问题若不在状态模型中单独区分，容易被误判为 \texttt{CE} 或 \texttt{WA}，误导学生将环境问题当作算法问题反复修改。

本文将部分系统级失败映射为 \texttt{SE} 状态，并在 \texttt{Submission.message} 中写入可读的错误摘要，同时保留已创建的提交记录，使前端能够展示一种失败但仍可追踪的状态。编译输出、运行输出和错误输出分别归入提交消息、Judge 轨迹与 JudgeRun 记录，避免将多种诊断信息混入同一字段。

在可观测性层面，判题链路应在日志中关联用户、题目、提交 id 与容器 id。本文实现已将提交 id 贯穿 \texttt{judge.ts}、Inngest 函数与编译运行模块，便于定位各阶段问题。当前实现未接入分布式追踪系统，但在关键异常分支打印了结构化日志字段，便于判断瓶颈出自异步函数吞吐不足还是单机 Docker 资源耗尽\cite{kubernetesdocs}。

\begingroup
\small
\setlength{\LTleft}{\fill}
\setlength{\LTright}{\fill}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.18\textwidth}P{0.34\textwidth}P{0.40\textwidth}}
\caption{判题异常分类与学生可见反馈策略}\\
\toprule
异常类别 & 典型诱因 & 反馈策略 \\
\midrule
\endfirsthead
\multicolumn{3}{c}{续表\thetable}\\
\toprule
异常类别 & 典型诱因 & 反馈策略 \\
\midrule
\endhead
\bottomrule
\endfoot
编译器报错 & 语法错误、链接错误 & 返回 \texttt{CE}，展示截断后的编译器输出。 \\
运行期错误 & 越界、除零、未定义行为触发崩溃 & 返回 \texttt{RE}，尽量保留运行期错误摘要。 \\
资源超限 & 死循环、极端数据导致 TLE/MLE & 返回 \texttt{TLE}/\texttt{MLE}，提示检查算法与边界。 \\
答案不一致 & 逻辑错误、格式未按题目要求输出 & 返回 \texttt{WA}，可展示首个失败用例差异。 \\
环境/配置故障 & Docker/镜像/数据缺失、远程连接失败 & 返回 \texttt{SE}，提示管理员检查部署与题目配置。 \\
\bottomrule
\end{longtable}
\endgroup

\section{AI 辅助子系统的流式响应与上下文构造实现}

AI 辅助子系统独立于主判题链路，在代码提交和编辑两个时机为学生提供解释、提示和对话支持。本节按总体架构、模型抽象、流式响应、结构化落库、编辑期同步辅助、上下文构造、边界与兜底七个层次展开。其中流式响应采用 \texttt{SSE} 方案，使聊天面板能够逐字输出回复，这与第四章异步判题的状态推送同属长连接增量推送。

\subsection{子系统总体架构与三类入口}

本系统将所有 AI 调用汇聚到统一的模型抽象层，前端对应三个独立入口：提交后异步分析、编辑期同步辅助和上下文聊天。

提交后异步分析由 \path{src/app/actions/analyze-code.ts} 的 \texttt{analyzeCode} Server Action 提供，在提交记录创建后触发，结果持久化为 \texttt{CodeAnalysis} 记录。编辑期同步辅助由 \path{src/app/actions/analyze.ts} 和 \path{src/app/actions/ai-improve.ts} 中的相关 Server Action 提供，被工具栏按钮直接调用并以 JSON 形式同步返回。上下文聊天由 \path{src/app/api/chat/route.ts} 的 Route Handler 配合浏览器侧 \texttt{useChat} 钩子实现，是三个入口中唯一采用流式响应的\cite{aisdk2024docs,aisdkreact2024docs}。

\begin{figure}[htbp]
  \centering
  \resizebox{0.96\textwidth}{!}{\input{figures/ai_subsystem_architecture.tex}}
  \caption{AI 辅助子系统总体架构示意}
  \label{fig:ch5-ai-arch}
\end{figure}

图~\ref{fig:ch5-ai-arch} 展示了三者在用户感知上的差异：提交后分析表现为几秒后自动浮现评估卡片，编辑期辅助为按钮停止加载后弹出 Diff 视图，上下文聊天则需要字符实时浮现。这些差异源于后端 API 形态：前两者使用 \texttt{generateText} 一次性返回，第三者必须用 \texttt{streamText} 建立长连接。提交后分析虽然围绕同一个 \texttt{submissionId} 展开，但不参与 \texttt{AC}/\texttt{WA} 等状态裁决。

\subsection{模型抽象层与 OpenAI 兼容 Provider 接入}

本系统未硬编码具体厂商 SDK，而是通过 Vercel AI SDK 的 Provider 抽象统一访问远端模型\cite{aisdk2024docs}。\path{src/lib/ai.ts} 中的 \texttt{createOpenAICompatible} 读取环境变量 \texttt{AI\_API\_KEY}、\texttt{AI\_BASE\_URL}、\texttt{AI\_MODEL}，生成统一的 \texttt{model} 实例供各模块复用。

需要切换模型时，只需修改 \texttt{AI\_BASE\_URL} 和 \texttt{AI\_MODEL} 即可，因为主流模型服务商均提供与 OpenAI 兼容的 \texttt{/v1/chat/completions} 端点\cite{deepseek2024api}。本文实测使用 DeepSeek 服务。\texttt{API\_KEY} 通过 \texttt{getRequiredEnv} 从进程环境变量读取，缺失时直接报错，避免延后失败\cite{owasp2021top10}。在图~\ref{fig:ch5-ai-arch} 中，模型层对应中部的统一模型层，所有调用方仅依赖 \texttt{generateText}、\texttt{streamText}、\texttt{tool} 三个 SDK 顶层函数，与具体 Provider 解耦。

\subsection{基于 SSE 的聊天流式响应实现}\label{sec:ch5-ai-sse}

服务端入口位于 \path{src/app/api/chat/route.ts}，解析对话历史 \texttt{messages} 后加入系统提示词，调用 \texttt{streamText} 获取流式输出，再通过 \texttt{toDataStreamResponse} 封装返回。同时设置 \texttt{maxDuration = 30} 约束长连接的最长执行时间\cite{nextjsdocs}。

浏览器侧由 \path{src/features/problems/bot/components/form.tsx} 配合 \texttt{@ai-sdk/react} 的 \texttt{useChat} 钩子完成增量渲染，组件消费 \texttt{messages}、\texttt{input} 和 \texttt{handleSubmit} 等状态。新文本片段到达时，钩子更新消息列表，页面重新渲染，并通过 \texttt{MdxPreview} 展示为富文本。

\subsection{提交后异步分析与 Tool Calling 强约束}

提交后异步分析在 \path{src/app/actions/analyze-code.ts} 中实现，是 \texttt{CodeAnalysis} 表的唯一写入方。其难点不在于调用模型本身，而在于让概率性输出稳定落到关系型表的特定列上。

\texttt{analyzeCode} 进入函数后立即创建一条 \texttt{status="QUEUED"} 的 \texttt{CodeAnalysis} 记录，在 \texttt{try} 块内推进到 \texttt{PROCESSING}，调用模型，并在工具回调内完成 \texttt{COMPLETED} 写入，异常则被 \texttt{catch} 捕获并将状态置为 \texttt{FAILED}。分析任务由 Server Action 在提交受理后异步触发，只写入 \texttt{CodeAnalysis} 表，不阻塞判题事件。因此即使 AI 服务故障，学生仍可拿到 \texttt{AC}/\texttt{WA} 等结果，仅看不到分析卡片。

为避免模型以自由文本形式返回 JSON 再走正则解析，本实现采用 Vercel AI SDK 的 \texttt{tool} 与 \texttt{toolChoice} 联合机制\cite{aisdk2024docs}。在 \texttt{generateText} 调用中声明唯一一个名为 \texttt{saveCodeAnalysis} 的工具，其入参由 \texttt{zod} 描述为一组结构化字段，包括时间复杂度、空间复杂度、五项 0 至 100 整数分数与一段长文本反馈，回调函数直接以这些字段更新 \texttt{CodeAnalysis} 记录并将状态推进到 \texttt{COMPLETED}。同时通过 \texttt{toolChoice} 指定该工具为强制调用项，不允许模型产生自由文本。

\begingroup
\small
\setlength{\LTleft}{\fill}
\setlength{\LTright}{\fill}
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.20\textwidth}P{0.22\textwidth}P{0.46\textwidth}}
\caption{CodeAnalysis 结构化结果字段设计}\label{tab:ch5-codeanalysis-fields}\\
\toprule
字段 & 类型或取值特征 & 作用说明 \\
\midrule
\endfirsthead
\multicolumn{3}{c}{续表\thetable}\\
\toprule
字段 & 类型或取值特征 & 作用说明 \\
\midrule
\endhead
\bottomrule
\endfoot
\texttt{status} & \texttt{QUEUED / PROCESSING / COMPLETED / FAILED} & 跟踪分析任务本身的执行状态，使 AI 流程能够独立于判题状态被观察与管理。 \\
\texttt{timeComplexity} & 枚举化复杂度取值 & 对学生代码的时间复杂度进行结构化记录，便于页面直接展示。 \\
\texttt{spaceComplexity} & 枚举化复杂度取值 & 保存空间复杂度分析结果，与时间复杂度共同构成算法层面的摘要信息。 \\
\texttt{overallScore} & 0--100 整数 & 对代码整体质量进行综合评价，用于形成直观的总览分值。 \\
\texttt{styleScore} & 0--100 整数 & 从命名、格式和结构整洁度等角度评价代码风格。 \\
\texttt{readability}\allowbreak\texttt{Score} & 0--100 整数 & 评价代码是否便于阅读和理解，突出教学反馈价值。 \\
\texttt{efficiencyScore} & 0--100 整数 & 评价算法与实现层面的效率水平，补充复杂度标签之外的解释信息。 \\
\texttt{correctness}\allowbreak\texttt{Score} & 0--100 整数 & 在不替代客观判题的前提下，给出模型对逻辑正确性的辅助判断。 \\
\texttt{feedback} & 长文本 & 保存面向学生的解释性反馈、问题定位和改进方向，是 AI 模块最直接的教学输出。 \\
\bottomrule
\end{longtable}
\endgroup

\subsection{编辑期同步辅助：复杂度分析与代码优化}

编辑器工具栏的复杂度分析按钮与代码优化按钮分别由 \path{src/app/actions/analyze.ts} 中的 \texttt{analyzeComplexity} 和 \texttt{optimizeCode} 实现，二者均使用非流式 \texttt{generateText}，再进行 JSON 解析。

复杂度分析通过 \texttt{system} 提示词限定 \texttt{Complexity} 的八个枚举值（\texttt{O(1)}、\texttt{O(logN)}、\texttt{O(N)} 等），要求模型返回 \texttt{\{"time":"O(N)","space":"O(1)"\}} 格式，返回后用 \texttt{safeParse} 校验。这种提示词限定枚举、解析端校验的策略，确保最终字段属于预定义集合，避免 \texttt{O(N\textsuperscript{2})}、\texttt{Linear} 等不规范字符串导致渲染失败。

代码优化按钮的逻辑位于 \path{ai-improve.ts}。提示词包含当前代码、可选错误信息和题目描述，同时附带题目对应的语言模板代码。输出格式限定为 \texttt{\{optimizedCode, explanation, issuesFixed\}} 的 JSON。考虑到模型可能将结果包裹在 json 围栏中，实现先用正则提取代码块内的 JSON，提取不到则回退到全文第一个花括号内容，最后通过 \texttt{safeParse} 执行校验。

优化结果返回前端后存入 \texttt{useProblemEditorStore} 的 \texttt{optimizedCode} 字段，不会直接覆盖原代码，允许学生在普通编辑器与 Diff 编辑器之间切换查看改动。

\subsection{聊天面板的上下文构造与题面注入}

聊天面板初始化时放入一条 \texttt{role: "system"} 的题面消息，每次提交时将当前编辑器代码用 Markdown 围栏包装为另一条 \texttt{role: "system"} 消息，追加到对话末尾后触发请求。

题面在用户进入页面时即固定，作为初始系统消息可避免重复传输；代码会被频繁修改，每次提问时拼入当前代码，保证模型看到的是用户此刻的版本。

题面与代码使用 \texttt{system} 消息而非 \texttt{user} 消息，是因为 OpenAI 协议中 \texttt{system} 消息的语义是模型应将其作为既定背景理解，与用户提问相分离\cite{deepseek2024api}，避免模型把题面和代码反复抄回给回答。结合要求以用户当前语言回复的提示词，模型既能知晓题目与代码，又能用用户熟悉的语言给出简洁回答。

\subsection{故障兜底与教学边界}

AI 子系统的失败兜底分为三层：第一，所有 AI 调用以 Server Action 或独立 Route Handler 形式存在，其结果不参与 \texttt{Submission.status} 的状态裁决，即使模型不可达或返回格式错误，学生仍能获得标准的 \texttt{AC}/\texttt{WA} 等结果。第二，\texttt{analyze-code.ts} 中所有 zod 字段均为 \texttt{optional}，模型返回缺少某些维度时，仅部分字段为空，前端显示占位即可。第三，所有失败分支将状态写为 \texttt{FAILED} 并保留时间戳，配合日志可定位问题阶段。

教学边界方面，本子系统遵守第三章的 AI 可控性原则：聊天面板的 system prompt 要求用用户语言回复且保持简洁；优化代码不覆盖原代码，需用户主动触发；分析结果与判题状态分离存储，阻断 AI 评分污染判题结果的可能。AI 在系统中始终扮演解释与提示的角色，而非裁判。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.88\linewidth]{images/fig5-7.png}
  \caption{AI 辅助反馈页面截图}
  \label{fig:ch5-ai-feedback}
\end{figure}

实际运行结果表明，系统能够展示 AI 分析卡片、雷达图、综合评分和文本反馈。图~\ref{fig:ch5-ai-feedback} 对应一份结果正确但效率较低的暴力枚举提交，系统给出 \(O(N^2)\) 的时间复杂度、\(O(1)\) 的空间复杂度以及 65 分综合评分，并在反馈中给出使用哈希表优化的方向。

\section{一体化工作区与交互}

\subsection{课程与作业组织实现}

除评测核心流程外，系统已实现面向教学组织的课程与作业基础功能。数据层包含课程、选课关系、作业及作业题目关联等核心结构，提交记录可关联作业上下文，业务层支持教师创建课程、维护学生名单、创建并发布作业、查看作业统计，学生可按课程视角进入作业并完成提交。该部分在系统中已形成可用流程，本文在此给出概要说明，详细交互流程与页面细节不再逐页展开。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-8.png}
  \caption{教师端课程页面中的学生管理与作业创建}
  \label{fig:ch5-course-page}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-9.png}
  \caption{教师端作业页面中的发布状态与统计概览}
  \label{fig:ch5-assignment-page}
\end{figure}

图~\ref{fig:ch5-course-page} 与图~\ref{fig:ch5-assignment-page} 分别展示了课程页与作业页的实际运行界面，可作为课程组织、作业发布与班级统计能力已接入业务流程的页面级证据。

\subsection{前端工作区组织与首页入口}

平台前端整体采用面向任务求解的一体化工作区组织方式：在用户进入具体题目之前，由首页承载平台定位、语言/主题切换与功能导航，进入题目页后，则将题面描述、代码编辑、提交记录、题解、测试用例、提交详情以及 AI 助手面板组合到统一视图，见图~\ref{fig:problem-workspace}，使学生无需在多个独立页面之间来回切换即可完成读题、编码、提交、查看判题与获取反馈这一完整过程。这种工作区式交互更符合程序设计训练的使用习惯，后文涉及的 LSP 诊断、AI 助手面板与 Diff 视图等功能也都挂载在同一工作区内，学生编码时不必跳到其他页面去触发。

首页采用与工作区相互呼应的两层组织：移动端保持纵向单列、保证核心入口可达，桌面端则以卡片形式概括移动端适配、Docker 运行环境与语言服务等关键功能，便于新用户在做题前先建立平台功能的整体认知。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-10.png}
  \caption{题目求解主界面截图}
  \label{fig:problem-workspace}
\end{figure}

\begin{figure}[htbp]
  \centering
  \begin{subfigure}[t]{0.24\textwidth}
    \centering
    \includegraphics[width=\linewidth]{images/fig5-11.png}
    \caption{移动端首页}
    \label{fig:home-mobile}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.72\textwidth}
    \centering
    \includegraphics[width=\linewidth]{images/fig5-12.png}
    \caption{桌面端功能导览区域}
    \label{fig:home-guide}
  \end{subfigure}
  \caption{首页入口的移动端与桌面端展示}
\end{figure}

\subsection{国际化、多语言与主题一致性}

平台的多语言支持由数据层与界面层共同构成，数据层借助 \texttt{ProblemLocalization} 按 \texttt{locale} 组织题目标题、题面描述与题解说明，使同一道题目在不同语言下呈现一致的题面结构，界面层则需要系统化的国际化 i18n 组织来承载导航、按钮、错误提示、判题状态解释、AI 面板标题等高频文本，否则会出现题面是英文但按钮仍是中文这一类割裂体验。

界面 i18n 工程实现采用 \texttt{next-intl} 与 Next.js App Router 结合：服务端组件通过 \texttt{getTranslations}/\texttt{getLocale} 读取当前语言环境下的文案，客户端组件通过 \texttt{useTranslations}/\texttt{useLocale} 绑定交互控件标签，语言偏好通过 Cookie 在请求之间保持，使刷新与导航后仍能获得一致的语言上下文\cite{nextjsdocs}。

除语言外，工作区还实现了深浅色主题切换，并使普通页面、Monaco 编辑器与差异对比编辑器使用对应主题，避免题面区域、代码区域与 Diff 视图在切换主题后出现明显割裂，这种语言与主题协同收敛的处理方式满足了长时间阅读、对比代码的基本需求。第六章交互测试中的多语言切换项 I-02 即用于核验界面文本与题面语境的一致性，而不仅是验证语言切换功能本身。

\subsection{Monaco 编辑器与基于 LSP 的 IDE 级语言服务}

本节会结合具体实现代码，来说明编辑器功能与语言服务链路的实际落地方式，重点聚焦那些和编码体验直接相关的关键模块。

许多传统在线评测系统仍采用轻量文本框或简化编辑器，常见功能止步于关键字着色与基础缩进。对入门学生而言，这类编辑器在拼写错误、漏写分号、括号不匹配、类型不匹配等高频问题上缺乏即时反馈，学生往往只能依赖编译器输出的大量信息自行定位，在题目规模变大或模板宏较多时，这种反馈链路会显著拉长试错周期。工程实现策略是将浏览器端编辑体验尽量对齐本地 IDE：以微软 Monaco Editor 作为代码编辑内核\cite{monacoeditorrepo}，并通过 Language Server Protocol 即 LSP 与独立的 C/C++ 语言服务器通信，使补全、诊断、悬停提示、签名帮助等功能以标准化方式进入 Web 工作区\cite{lsp2024spec,monacolanguageclientrepo}。

\noindent 前端依赖与编辑器基础能力。题目求解页的实现栈以 \texttt{@monaco-editor/react} 与 \texttt{monaco-editor} 为核心，并配合 \texttt{monaco-languageclient}、\texttt{vscode-languageclient} 与 \texttt{vscode-ws-jsonrpc} 将 LSP 的 JSON-RPC 消息映射到浏览器 \texttt{WebSocket} 传输层\cite{monacolanguageclientrepo}。编辑器默认选项在 \path{src/config/editor.ts} 中集中配置，启用 ligatures、括号配对着色、缩进参考线、粘性滚动、平滑滚动与自动换行等特性，使长时间读码与改码的视觉负担更低。主题方面，工程使用 Shiki 高亮器并通过 \texttt{@shikijs/monaco} 将配色注册到 Monaco，从而使代码区与站点深浅色主题保持一致，与图~\ref{fig:dark-theme-workspace} 所示工作区观感一致。

\noindent 语言服务端点注入与连接建立。语言服务的连接参数并不写死在前端常量中，而是由服务端在渲染题目页时从数据库读取并注入。具体而言，\path{src/features/problems/code/components/content.tsx} 在服务端执行 \texttt{prisma.languageServerConfig.findMany()}，将返回的 \texttt{LanguageServerConfig} 列表传入 \texttt{ProblemEditor}，再下发到 \texttt{CoreEditor}\cite{prismadocs}。数据模型在 \texttt{schema.prisma} 中以语言为主键记录 \texttt{protocol}、\texttt{hostname}、\texttt{port} 与 \texttt{path} 等字段，使同一套前端代码可以在本地 Compose、内网部署或公网域名等不同环境下复用，只需调整种子数据或数据库配置即可。

在浏览器侧，\path{src/components/core-editor.tsx} 根据当前语言选择匹配的 \texttt{LanguageServerConfig}，拼接出 \texttt{WebSocket} URL 并建立连接，连接建立后创建 \texttt{MonacoLanguageClient} 并启动，使 Monaco 与语言服务器进入同一套 LSP 会话。编辑器挂载完成后才发起连接，卸载时停止 client，避免页面切换造成资源泄漏。工具栏中的 \texttt{LSP} 状态指示组件 \path{lsp-connection-indicator.tsx} 通过观察 \texttt{WebSocket} 的就绪状态给出可视化反馈，让学生明确当前是否具备语言级诊断能力，这与仅依赖提交后判题反馈的体验有本质差异。

\noindent 容器化语言服务与 Compose 编排。语言服务器本身同样以容器方式运行，便于与主服务生命周期对齐。工程 \texttt{compose} 配置中除 \texttt{judge4c} 与 \texttt{postgres} 外，还包含 \texttt{lsp-c} 与 \texttt{lsp-cpp} 两个服务镜像。二者基于 \path{docker/lsp/clangd} 构建，在容器内通过 \texttt{jsonrpc-ws-proxy} 将 \texttt{WebSocket} JSON-RPC 转发到 \texttt{clangd} 进程，\texttt{servers.yml} 中声明 \texttt{/usr/bin/clangd} 及后台索引参数。主服务通过 \texttt{depends\_on} 等待语言服务健康检查通过后再启动，从而降低页面已打开但语言服务尚未就绪的偶发体验问题。该编排方式也与前文所述判题执行与辅助能力尽量容器化、可替换的总体思路一致\cite{dockerdocs,kubernetesdocs}。

\begin{figure}[htbp]
  \centering
  \resizebox{0.96\textwidth}{!}{\input{figures/lsp_client_architecture.tex}}
  \caption{Monaco 编辑器与容器化 \texttt{clangd} 语言服务的连接关系示意}
  \label{fig:ch5-lsp-arch}
\end{figure}

\subsection{响应式布局与多终端适配}

前端除桌面主场景外，也实现了平板与移动端的退化布局策略，系统会根据视口宽度切换交互组织：桌面端保留多面板并列与可折叠侧栏，移动端则将侧边内容收纳到抽屉面板，避免窄屏固定多栏造成拥挤。换言之，平台虽然以桌面编程训练为主，但小屏设备仍可完成核心操作链路。

\subsection{运行环境与本地部署}

平台采用多容器编排：\texttt{judge4c} 主服务承载页面访问、业务处理与判题入口，\texttt{postgres} 承担业务数据持久化，\texttt{redis} 作为 Inngest 与 Soketi 的共享基础设施，\texttt{inngest} 负责事件函数编排，\texttt{soketi} 负责判题状态实时推送，\texttt{lsp-c}/\texttt{lsp-cpp} 为 C/C++ 编辑器提供语言服务。主服务通过 \texttt{depends\_on} 等待数据库、Redis、Inngest 与语言服务健康检查通过后再启动，降低初始化阶段的偶发失败。当前镜像基础层采用 \texttt{node:22-bookworm-slim}，并安装 \texttt{openssl}、\texttt{ca-certificates} 与 \texttt{curl}，以满足 Prisma/OpenSSL 运行依赖和容器健康检查需要。判题执行位置由配置项区分为本地 Docker 与远程 Docker 两种模式：本地模式直接经宿主机 Docker 套接字管理容器，远程模式则按协议、地址、端口与 TLS 证书建立连接，从而把业务服务部署位置与判题执行位置解耦，为判题节点上云提供直接基础。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/fig5-13.png}
  \caption{系统本地运行与开发环境截图}
  \label{fig:local-dev-env}
\end{figure}

本地端到端构建验证结果如下：\texttt{docker compose -f compose.local.yml up -d} 启动后 \texttt{postgres}、\texttt{redis}、\texttt{inngest}、\texttt{soketi} 与主服务均进入可用状态，PostgreSQL 初始化阶段会通过 \path{init.sql} 额外创建 Inngest 使用的数据库，\texttt{bun install} 完成依赖安装并触发 Prisma Client 生成，\texttt{bunx prisma migrate deploy} 应用全部 15 个数据库迁移，\texttt{bunx prisma db seed} 完成种子数据初始化，\texttt{bun run build} 生产构建通过，构建输出中可见 \path{/problemset}、\path{/problems/[problemId]}、\path{/dashboard} 以及后台管理相关核心路由均已参与构建。图~\ref{fig:local-dev-env} 给出了运行界面与开发终端状态，可作为本地演示环境正常工作的证据。

\section{本章小结}

本章围绕平台核心模块的实现结果，说明了数据模型、题库组织、在线提交、容器化判题、AI 辅助分析、前端工作区、身份认证与 LSP 等关键内容。结果表明，系统已可在单机环境完成从登录、做题、提交到结果反馈的完整流程，课程与作业组织能力也已形成基本流程，异步判题与实时状态推送也已接入核心流程。
