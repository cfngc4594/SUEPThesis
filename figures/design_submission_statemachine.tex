% 第4章：提交判题主要状态迁移（简化，与 PD/CP/RU/AC 等码一致）
\begin{tikzpicture}[
  font=\scriptsize\sffamily,
  >=Stealth,
  s/.style={draw, rounded corners=2pt, inner sep=3pt, minimum width=1.05cm, minimum height=0.65cm, align=center}
]
  \node[s, fill=blue!6] (PD) {PD};
  \node[s, right=of PD, fill=blue!6] (QD) {QD};
  \node[s, right=of QD, fill=yellow!15] (CP) {CP};
  \node[s, below=0.9cm of CP, fill=red!10] (CE) {CE};
  \node[s, right=of CP, fill=green!12] (CS) {CS};
  \node[s, right=of CS, fill=yellow!15] (RU) {RU};
  \node[s, above right=0.6cm and 0.4cm of RU, fill=green!20] (AC) {AC};
  \node[s, right=0.2cm of RU, fill=red!8] (WA) {WA};
  \node[s, below=0.55cm of RU, fill=red!8] (RE) {RE};
  \node[s, below right=0.55cm and 0.2cm of RU, fill=red!8] (TLE) {TLE};
  \node[s, below=0.55cm of CE, fill=purple!10] (SE) {SE};

  \draw[->] (PD) -- (QD);
  \draw[->] (QD) -- (CP);
  \draw[->] (CP) -- node[left, font=\tiny]{编译失败} (CE);
  \draw[->] (CP) -- node[above, font=\tiny]{成功} (CS);
  \draw[->] (CS) -- (RU);
  \draw[->] (RU) -- node[above, font=\tiny]{全过} (AC);
  \draw[->] (RU) -- node[above, font=\tiny] {WA} (WA);
  \draw[->] (RU) -- node[left, font=\tiny] {RE} (RE);
  \draw[->] (RU) -- node[right, font=\tiny]{TLE} (TLE);
  \draw[->, dashed] (CP) -- ++(-1.6,-1.1) |- node[near start, above, font=\tiny]{环境故障} (SE);
\end{tikzpicture}
