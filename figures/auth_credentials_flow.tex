% 与 judge4c：Credentials + PrismaAdapter.createSession 路径一致（简图）
\begin{tikzpicture}[
  node distance=0.5cm,
  font=\scriptsize\sffamily,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=4pt, text width=3.85cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[box, fill=blue!6] (A) {浏览器\\CredentialsSignInForm.tsx\\提交邮箱与密码};
  \node[box, fill=green!8, below=of A] (B) {Server Action\\signInWithCredentials\\app/actions/auth.ts};
  \node[box, fill=orange!8, below=of B] (C) {\texttt{zod} 校验 + Prisma 查用户\\\texttt{bcrypt.compare} 比对口令};
  \node[box, fill=purple!8, below=of C] (D) {\texttt{signIn("credentials")}\\lib/auth.ts};
  \node[box, fill=gray!10, below=of D] (E) {Credentials.authorize\\再次校验并返回 User};
  \node[box, fill=cyan!8, below=of E] (F) {jwt 回调标记\\token.credentials=true};
  \node[box, fill=yellow!12, below=of F] (G) {自定义 jwt.encode\\createSession $\rightarrow$ PrismaAdapter\\写入 DB 会话（30 天）};
  \node[box, fill=blue!6, below=of G] (H) {会话 Cookie 生效\\router.push(callbackUrl)};
  \foreach \x/\y in {A/B,B/C,C/D,D/E,E/F,F/G,G/H} \draw[arr] (\x) -- (\y);
\end{tikzpicture}
