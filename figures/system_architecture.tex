% 系统总体架构（TikZ），供正文 \resizebox{..}{!}{\input{...}} 嵌入
\begin{tikzpicture}[
  font=\small\sffamily,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=6pt, minimum width=2.6cm},
  arr/.style={-{Stealth[length=2mm]}, thick}
]
  \node[box, fill=blue!8] (ui) {表现层\\Web 工作区};
  \node[box, fill=green!10, right=1.1cm of ui] (api) {业务服务层\\提交/题库/认证};
  \node[box, fill=orange!12, right=1.1cm of api] (judge) {评测执行层\\容器化判题};
  \node[box, fill=purple!10, right=1.1cm of judge] (ai) {AI 辅助层\\分析与问答};
  \node[box, fill=gray!12, below=1.0cm of $(api)!0.5!(judge)$] (db) {数据支撑层\\关系型数据库};

  \draw[arr] (ui) -- node[above, font=\scriptsize] {HTTPS} (api);
  \draw[arr] (api) -- node[above, font=\scriptsize] {调度/调用} (judge);
  \draw[arr] (api) -- node[above, font=\scriptsize] {异步任务} (ai);
  \draw[arr] (judge) -- node[right, font=\scriptsize, sloped] {结果写回} (api);
  \draw[arr] (ai) -- node[right, font=\scriptsize, sloped] {结构化结果} (api);
  \draw[arr] (api) -- node[left, font=\scriptsize] {持久化} (db);
  \draw[arr, dashed] (judge.south) -- ++(0,-0.35) -| node[near start, below, font=\scriptsize] {Docker / 远程引擎} (db.north east);
\end{tikzpicture}
